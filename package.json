{
  "name": "bearer-auth-service",
  "version": "1.0.0",
  "description": "Bearer authentication microservice with multi-tokens",
  "license": "MIT",
  "author": "orKL3mlz",
  "type": "module",
  "engines": {
    "node": ">=25"
  },
  "main": "dist/server.ts",
  "scripts": {
    "prepare": "npm run build",
    "dev": "ts-node-dev --respawn --transpile-only --exit-child --ignore-watch node_modules --no-notify /app/src/server.ts",
    "build": "tsc -p /app/tsconfig.json",
    "start": "node /app/dist/server.ts",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit -p /app/tsconfig.json"
  },
  "dependencies": {
    "express": "^4.21.1",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/morgan": "^1.9.9",
    "@types/node": "^20.11.30",
    "eslint": "^9.11.1",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.30.0",
    "prettier": "^3.3.3",
    "ts-node-dev": "^2.0.0"
  }
}